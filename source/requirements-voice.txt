# Optional dependencies required when producing an official voice-enabled build.
sounddevice==0.5.5
soxr==1.1.0
# Release builds install the hash-pinned custom PyAV wheel produced by
# packaging/clean_audio_runtime.py. Never install the upstream binary wheel.
transcribe-cpp==0.1.3
transcribe-cpp-native==0.1.3
llama-cpp-python==0.3.35
